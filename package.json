{
  "name": "html-to-blocks",
  "version": "1.0.5",
  "description": "Use this package when you have a need to accept html from a CMS and parse/transform it into components.",
  "main": "dist/index.js",
  "repository": "github:joeflateau/html-to-blocks",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "mocha -r ts-node/register src/**/*.spec.ts",
    "test:watch": "NODE_ENV=test mocha -r ts-node/register --watch-extensions ts --extension ts --watch --recursive src/**/*.spec.ts",
    "prepare": "npm run test && rm -rf ./dist && tsc"
  },
  "author": "Joe Flateau",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.2.11",
    "@types/jsdom": "^16.2.1",
    "@types/mocha": "^7.0.2",
    "chai": "^4.2.0",
    "jsdom": "^16.2.2",
    "mocha": "^7.1.2",
    "typescript": "^3.7.5"
  },
  "prettier": {
    "printWidth": 110,
    "trailingComma": "all"
  },
  "dependencies": {}
}
